"use strict";
// De momento está metido dentro de la función de timer, cuando el tiempo se termina
// Si lo pongo como está aquí, cuando JS evalua está condición es false, porque al principio gameOver es un párrafo vacío

// const gameOver = document.querySelector(".game-over");

// console.log(gameOver);
// console.log(timer.textContent);

// if (timer.innerHTML === "00:00") {
//     gameOver.classList.remove("hidden");
// }